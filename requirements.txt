# harness + web app (runs anywhere: laptop or RunPod)
requests>=2.31
python-dotenv>=1.0
Pillow>=10.0
Flask>=3.0
pandas>=2.0
tabulate>=0.9

# fal.ai hosted adapters (FLUX Virtual Try-On Pro, image-apps-v2)
fal-client>=0.5

# NOTE: the open-source model repos (IDM-VTON, Leffa, CatVTON) bring their own
# heavy deps (torch, diffusers, etc.) and are installed on the RunPod pod by
# runpod/setup.sh — NOT from this file.
